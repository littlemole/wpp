version: '2'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
      args:
        CXX: ${CXX}
        EVENTLIB: ${EVENTLIB}
        BUILDCHAIN: ${BUILDCHAIN}
        BASE_IMAGE: ${BASE_IMAGE}
    ports:
     - "9876:9876"
  redis:
    image: "redis:alpine"
    